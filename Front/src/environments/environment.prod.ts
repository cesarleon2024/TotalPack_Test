/*
import { environment } from '../environments/environment';
const apiUrl = environment.API_GetUsers;
*/

export const environment = {
    production: true,
    API_GetUsers: 'https://localhost:7073/api/User' // Cambiar la URL por la de producción cuando esté disponible
}; 